# Minimal runtime for CLI + training (matches pyproject core deps).
# Recommended for contributors: pip install -e ".[dev,notebook]"
numpy>=1.24.0,<3
pandas>=2.0.0,<3
scikit-learn>=1.3.0,<2
joblib>=1.3.0,<2

# Notebook & visualization (also available as: pip install -e ".[notebook]")
jupyter>=1.0.0
ipykernel>=6.25.0
matplotlib>=3.7.0,<4
seaborn>=0.13.0,<1
